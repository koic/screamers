require "spec_helper"

RSpec.describe Screamers do
  it "has a version number" do
    expect(Screamers::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
